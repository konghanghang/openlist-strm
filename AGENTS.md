# AI 协作指南

本文件用于约束 AI 在 OpenList-STRM 项目中的协作方式。

> `AGENTS.md` 与 `CLAUDE.md` 必须保持一致，不允许单独演化。
> 技术开发细则请参考 [docs/development-guide.md](docs/development-guide.md)。

## 核心原则

- **好品味**：优先调整数据结构、职责边界和调用关系，尽量消灭特殊情况。
- **Never break userspace**：未被明确要求改变的用户可见行为，默认必须保持不变。
- **实用主义**：只解决真实问题，不堆回退、备用、假兼容。
- **简洁**：函数短、职责单一；超过 3 层缩进时先反思设计。

## 项目上下文

- 项目名称：OpenList-STRM
- 技术栈：
  - 后端：Go 1.23 + Gin + GORM + SQLite
  - 前端：Vue 3 + Vite + Element Plus
- 主要目录：
  - `backend/`：Go 后端
  - `web/`：Vue 前端
  - `configs/`：配置文件
  - `deployments/`：部署相关文档和脚本
  - `docs/`：项目文档

## 沟通规则

- 使用中文输出；只在代码标识符、命令、路径、字段名等必须场景保留英文。
- 表达直接，批评只针对技术问题，不写空话。
- 遇到新任务或方向变化，先复述理解，再继续执行。

推荐格式：

```text
基于现有信息，我理解你的需求是：[换一种说法复述需求]
请确认我的理解是否准确？
```

## 决策与评审

### 决策输出

默认按下面结构组织：

- `【结论】`
- `【方案】`
- `【补充分析】`：仅在跨模块、兼容性敏感、复杂排查时展开
- `【需要澄清】`：信息不足时使用

### Review 规则

默认按 review 模式输出，先给总体判断：

```text
【品味评分】
🟢 好品味 / 🟡 凑合 / 🔴 垃圾

【致命问题】
- ...

【改进方向】
- ...
```

每条问题至少说明：

- 问题是什么
- 影响是什么
- 定位到哪个文件和行号
- 建议的最小改动是什么

如果没发现需要修复的问题，必须明确写出“本轮未发现需要修复的问题”。

以下场景不能只做补丁级 review，必须看完整链路：

- 跨模块改动
- 配置结构、启动入口、部署方式变更
- 调度、Webhook、异步任务链路改动
- 用户可见状态流转变化
- 数据库模型、持久化逻辑、迁移策略变化

## 工作规则

### 修改代码前

- 先确认改动触达的模块边界，不要一上来就改实现。
- 涉及目录调整时，先判断职责是否真的混在一起；不要为了“看起来整齐”搬文件。
- 允许为测试引入轻量依赖注入、可替换函数或最小 stub；禁止为测试引入超出问题范围的大抽象层。

### 前端改动

- 先确认页面职责、路由归属和接口契约，再动样式或交互。
- 没确认清楚现有行为前，不要直接重写页面。
- 文案、布局、状态展示的改动都要检查是否改变了用户操作路径。

### 后端改动

- 配置、调度、Webhook、文件生成链路属于关键路径，改动时必须保证可观测性。
- 日志要能说明走到哪一步、为什么进入该分支、失败点在哪里。
- 禁止输出密码、Token、完整敏感返回体。

### 文档规则

- `README.md` 是项目入口，不承担全部规范细节。
- `docs/development-guide.md` 负责开发阅读路径和文档判断规则。
- `docs/PRD.md` 负责产品需求、目标和较稳定的架构说明。
- `docs/TESTING.md` 负责测试策略、测试清单和覆盖进度。
- 新增稳定规则时，优先合并进现有文档；禁止随手创建“临时文档”。
- 修改文档时要顺手检查相关链接是否仍然有效。

## 验证与提交

### 提交前检查

- Go 改动后，在 `backend/` 下至少运行相关格式化、编译和测试命令。
- 前端改动后，在 `web/` 下至少运行构建验证。
- 没完成最小验证，不要宣称任务已经完成。

### 提交规则

- 不主动提交。
- 只有在用户明确同意后，才执行 `git commit`。
- 如果只是完成修改和验证，直接汇报结果，不要默认进入提交步骤。
